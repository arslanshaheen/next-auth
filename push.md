<!-- git add .
git commit -m "Added feature in new branch"
git push origin my-branch-name
git checkout master
git pull origin master  # get latest changes
git merge my-branch-name
git push origin master
 -->
